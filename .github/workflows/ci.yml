# Copyright 2026 BinBashMedium
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     https://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: CI

permissions:
  contents: read

on:
  push:
    branches: ["**"]
  pull_request:
  release:
    types: [published]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Unit tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - uses: gradle/actions/setup-gradle@v4

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Override Java home for Gradle
        run: echo "org.gradle.java.home=$JAVA_HOME" >> gradle.properties

      - name: Run domain unit tests
        run: ./gradlew :domain:test --no-daemon

      - name: Run app unit tests
        # -PskipPaparazziPlugin=true (1) skips registering the Paparazzi plugin so its
        # bytecode instrumentation and custom reporter don't interfere with unit tests, and
        # (2) causes build.gradle.kts to omit src/screenshotTest/kotlin from the test source
        # set, so ScreenshotTest.kt is never compiled and no Paparazzi types are needed.
        run: ./gradlew :app:test -PskipPaparazziPlugin=true --no-daemon

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: |
            domain/build/reports/tests/
            app/build/reports/tests/
          retention-days: 14

  screenshots:
    name: Generate screenshots
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - uses: gradle/actions/setup-gradle@v4

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Override Java home for Gradle
        run: echo "org.gradle.java.home=$JAVA_HOME" >> gradle.properties

      - name: Record Paparazzi screenshots
        run: ./gradlew :app:recordPaparazziDebug --no-daemon

      - name: Upload screenshots artifact
        uses: actions/upload-artifact@v4
        with:
          name: screenshots
          path: app/src/test/snapshots/images/
          retention-days: 14

  practice-screenshot:
    name: Capture Practice Screen screenshot (emulator)
    needs: test
    runs-on: ubuntu-latest
    continue-on-error: true  # emulator flakiness must not block release
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - uses: gradle/actions/setup-gradle@v4

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Override Java home for Gradle
        run: echo "org.gradle.java.home=$JAVA_HOME" >> gradle.properties

      - name: Enable KVM for hardware acceleration
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Run practice screenshot test on emulator
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 29
          arch: x86_64
          profile: pixel_5
          disable-animations: true
          script: |
            ./gradlew :app:connectedDebugAndroidTest \
              -Pandroid.testInstrumentationRunnerArguments.class=com.binbashmedium.sightreadingtrainer.ui.PracticeScreenshotTest \
              --no-daemon
            adb pull /sdcard/Android/data/com.binbashmedium.sightreadingtrainer/files/practice_screen.png ./practice_screen.png

      - name: Upload practice screenshot artifact
        uses: actions/upload-artifact@v4
        with:
          name: practice-screenshot
          path: practice_screen.png
          retention-days: 14

  build-debug:
    name: Build debug APK
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - uses: gradle/actions/setup-gradle@v4

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Override Java home for Gradle
        run: echo "org.gradle.java.home=$JAVA_HOME" >> gradle.properties

      - name: Build debug APK
        run: ./gradlew :app:assembleDebug --no-daemon

      - name: Upload debug APK (artifact)
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: app/build/outputs/apk/debug/app-debug.apk
          retention-days: 14

  release-apk:
    name: Upload APK to release
    if: github.event_name == 'release'
    needs: [test, screenshots, practice-screenshot]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - uses: gradle/actions/setup-gradle@v4

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Override Java home for Gradle
        run: echo "org.gradle.java.home=$JAVA_HOME" >> gradle.properties

      - name: Build debug APK
        run: ./gradlew :app:assembleDebug --no-daemon

      - name: Download menu screenshots (Paparazzi)
        uses: actions/download-artifact@v4
        with:
          name: screenshots
          path: release-screenshots/menu/

      - name: Download practice screenshot (emulator)
        continue-on-error: true  # practice-screenshot may have failed
        uses: actions/download-artifact@v4
        with:
          name: practice-screenshot
          path: release-screenshots/practice/

      - name: Zip all screenshots
        run: zip -r screenshots.zip release-screenshots/

      - name: Upload APK and screenshots to GitHub release
        uses: softprops/action-gh-release@v2
        with:
          fail_on_unmatched_files: false
          files: |
            app/build/outputs/apk/debug/app-debug.apk
            screenshots.zip
